public class Ecran {
    public static void affiche(Fraction fraction){
        System.out.println("le numerateur de cette fraction est : "+fraction.getNumerateur()+"\nLe denominteur de cette fraction est : "+fraction.getDenominateur());
    }
}
